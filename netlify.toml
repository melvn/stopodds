[build]
  base = "app/"
  command = "cd app && npm install && npm run build"
  publish = "app/.next"

[build.environment]
  NODE_VERSION = "18"
  NEXT_PUBLIC_API_URL = "https://stopodds-production.up.railway.app"